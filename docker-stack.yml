version: "3"

services:
  app:
    build: ./
    image: XinFin/governance
    restart: "on-failure:10"
    ports:
      - "3001:3001"
    depends_on:
      - mongodb

  mongodb:
    image: mongo:3.6
    restart: "on-failure:10"
    volumes:
      - "mongodb:/data/db"

volumes:
  mongodb:
